import { Camera, Vector3 } from '@babylonjs/core';
export declare const computeVertigo: (camera: Camera, focusPosition: Vector3, perspective: number, height: number, aspect: number, useRightHandedSystem?: boolean) => void;
